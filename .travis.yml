language: php
php:
  - 5.5
  - 5.6
  - hhvm
  - nightly
matrix:
  allow_failures:
    - php: nightly
    
before_script:
  - composer self-update

install:
  - composer install --prefer-source --no-interaction --dev
script:
  - vendor/bin/phpspec run -v
